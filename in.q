def main() => void{
    #/this is a
    multi line comment/#
    int a;
    int b;
    # this is a single line comment
    int c;
    if((a>5)) {
        c=((b+a)*a);
    }
    else {
        c=((b+a)-a);
    }
    #this is a single line comment
}
