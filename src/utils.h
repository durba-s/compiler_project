#include <bits/stdc++.h>
#include <string>
#include <regex>
using namespace std;

string getNextToken();

char getChar();
void addChar();

bool isDelimiter();
bool isKeyword();
bool isIdentifier();
string isLiteral();
